import { FieldInterface } from './types';
declare const Reference: ({ options, fieldId, keyProperty, displayProperty, placeholder, className, formId, }: FieldInterface) => JSX.Element;
export default Reference;
